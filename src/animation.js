class Animation {
    constructor(user_id, nickname, length, song, background) {
        this.nickname = nickname;
        this.length = length;
        this.song = song;
        this.background = background;
        this.user_id = user_id
    }
}